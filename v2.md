# Closed Marketplace Data Ingestion Plan

## Context

FRAX currently syncs data via APIs from 9 marketplaces. Indian marketplaces like **Meesho, Myntra, Nykaa, JioMart, Ajio, Udaan, IndiaMart** have no public seller APIs. We need alternative data ingestion methods so sellers on these platforms can use FRAX.

**Goal**: 3-phase rollout — CSV Upload (MVP) → Browser Extension → Email Parsing.

Data from all methods lands in the **same UnifiedOrder/UnifiedProduct tables**, so existing metrics, alerts, AI chat, and briefings work automatically with zero changes.

---

## Phase 1: CSV Upload (MVP — Build Now)

### 1.1 Schema Changes

**File: `prisma/schema.prisma`**

Add 7 new marketplace types to enum:
```prisma
enum MarketplaceType {
  // ... existing 12 ...
  MEESHO
  MYNTRA
  NYKAA
  JIOMART
  AJIO
  UDAAN
  INDIAMART
}
```

Add `sourceType` field to UnifiedOrder and UnifiedProduct:
```prisma
model UnifiedOrder {
  // ... existing fields ...
  sourceType  String?  // null = API sync, "csv" = CSV upload, "extension" = browser ext, "email" = email parse
}

model UnifiedProduct {
  // ... existing fields ...
  sourceType  String?
}
```

New model to track CSV imports:
```prisma
model CsvImport {
  id             String   @id @default(cuid())
  userId         String
  user           User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  connectionId   String
  connection     MarketplaceConnection @relation(fields: [connectionId], references: [id], onDelete: Cascade)
  marketplace    MarketplaceType
  entity         String   // "orders" | "products"
  fileName       String
  fileHash       String   // SHA-256 of file contents — prevents duplicate imports
  totalRows      Int
  importedRows   Int      @default(0)
  skippedRows    Int      @default(0)
  errorRows      Int      @default(0)
  errors         Json?    // Array of { row: number, field: string, message: string }
  columnMapping  Json?    // User's confirmed column mapping
  status         String   @default("pending") // pending | processing | completed | failed
  createdAt      DateTime @default(now())
  completedAt    DateTime?

  @@unique([userId, fileHash])
  @@index([userId, marketplace])
}
```

### 1.2 New Dependency

```bash
npm install papaparse
npm install -D @types/papaparse
```

PapaParse — simple, battle-tested CSV parser. Works in Node.js, handles edge cases (quoted commas, multiline fields, BOM).

### 1.3 Per-Marketplace CSV Column Mappings

**File: `src/lib/csv/mappings.ts`** (new)

Each marketplace exports CSVs with different column headers. Define mappings with multiple alternative names per field (marketplaces change headers without notice):

```
MEESHO Orders:
  "Sub Order No" / "Order ID"          → externalOrderId
  "Product Name"                        → itemTitle
  "Quantity"                            → quantity
  "Supplier Price" / "Order Amount"     → totalAmount
  "Order Date"                          → orderedAt
  "Order Status"                        → status
  Currency: "INR" (hardcoded)

MYNTRA Orders:
  "Order No" / "Order Number"           → externalOrderId
  "Style Name" / "Product Name"         → itemTitle
  "Qty" / "Quantity"                    → quantity
  "MRP" / "Selling Price"              → totalAmount
  "Order Created On" / "Order Date"     → orderedAt
  "Current Status"                      → status
  Currency: "INR" (hardcoded)

NYKAA Orders:
  "Order ID"                            → externalOrderId
  "Product Name"                        → itemTitle
  "Qty"                                 → quantity
  "Selling Price" / "Amount"            → totalAmount
  "Order Date"                          → orderedAt
  "Status"                              → status
  Currency: "INR" (hardcoded)

JIOMART Orders:
  "Order Id" / "Order Number"           → externalOrderId
  "Item Name" / "Product"               → itemTitle
  "Quantity"                            → quantity
  "Total Amount" / "Order Value"        → totalAmount
  "Created Date" / "Order Date"         → orderedAt
  "Order Status"                        → status
  Currency: "INR" (hardcoded)

AJIO Orders:
  "Order Number"                        → externalOrderId
  "Product Name" / "Style Name"         → itemTitle
  "Quantity"                            → quantity
  "Selling Price" / "Amount"            → totalAmount
  "Order Date"                          → orderedAt
  "Status"                              → status
  Currency: "INR" (hardcoded)

UDAAN Orders:
  "Order ID"                            → externalOrderId
  "Product Name"                        → itemTitle
  "Qty" / "Quantity"                    → quantity
  "Total Price" / "Amount"              → totalAmount
  "Order Date" / "Created At"           → orderedAt
  "Status"                              → status
  Currency: "INR" (hardcoded)

INDIAMART Orders:
  "Query ID" / "Lead ID"               → externalOrderId
  "Product Name"                        → itemTitle
  "Quantity" (default: 1)               → quantity
  "Estimated Order Value" (default: 0)  → totalAmount
  "Date" / "Query Date"                 → orderedAt
  Status: "PENDING" (default — leads)
  Currency: "INR" (hardcoded)
```

Product mappings follow a similar pattern (SKU, Title, Price, Stock/Inventory).

### 1.4 API Routes

#### `POST /api/import/preview` (new)
- Accepts multipart FormData: `file` (CSV) + `marketplace` (string)
- Validates: file type (CSV only), size (max 10MB), row count (max 50,000)
- Computes SHA-256 file hash → checks CsvImport for duplicate
- Parses first 5 rows with PapaParse
- Auto-matches CSV columns to marketplace mapping
- Returns: `{ columns: [...], sampleRows: [...], suggestedMapping: {...}, totalRows: N }`

#### `POST /api/import/confirm` (new)
- Accepts JSON: `{ marketplace, fileHash, columnMapping, entity: "orders"|"products" }`
- Creates MarketplaceConnection (if first import for this marketplace)
- Creates CsvImport record with status "processing"
- Creates UnifiedSyncLog with trigger "manual"
- Parses full CSV, validates each row, upserts to UnifiedOrder/UnifiedProduct
- Skips rows with missing required fields (logs errors in CsvImport.errors)
- Returns: `{ importId, imported: N, skipped: N, errors: [...] }`

#### `GET /api/import/history` (new)
- Returns list of CsvImport records for the user
- Includes: marketplace, fileName, importedRows, errorRows, createdAt

### 1.5 Frontend Components

#### Import Page: `src/app/dashboard/import/page.tsx` (new)
Three-step flow:

**Step 1 — Upload**
```
┌─────────────────────────────────────────┐
│  Import Data                            │
│                                         │
│  Select Marketplace: [Meesho ▼]         │
│                                         │
│  Import Type: ○ Orders  ○ Products      │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  📁 Drag & drop CSV file here   │    │
│  │     or click to browse          │    │
│  └─────────────────────────────────┘    │
│                                         │
│  💡 Download from Meesho Seller Panel   │
│     → Orders → Download Report          │
└─────────────────────────────────────────┘
```

**Step 2 — Preview & Map**
```
┌─────────────────────────────────────────┐
│  Preview Import — 1,234 rows found      │
│                                         │
│  Column Mapping:                        │
│  CSV "Sub Order No" → [Order ID ▼]      │
│  CSV "Product Name" → [Item Title ▼]    │
│  CSV "Order Amount" → [Total Amount ▼]  │
│  CSV "Order Date"   → [Order Date ▼]    │
│  CSV "Status"       → [Status ▼]        │
│                                         │
│  Sample Data (first 5 rows):            │
│  ┌────────┬───────────┬─────┬──────┐    │
│  │ Order  │ Product   │ Amt │ Date │    │
│  ├────────┼───────────┼─────┼──────┤    │
│  │ MSH001 │ T-Shirt   │ 499 │ 2/15 │    │
│  │ MSH002 │ Jeans     │ 899 │ 2/14 │    │
│  └────────┴───────────┴─────┴──────┘    │
│                                         │
│  ⚠ 3 rows have missing Order ID        │
│                                         │
│  [Cancel]              [Import 1,231 →] │
└─────────────────────────────────────────┘
```

**Step 3 — Result**
```
┌─────────────────────────────────────────┐
│  ✅ Import Complete                     │
│                                         │
│  Imported: 1,231 orders                 │
│  Skipped:  3 (missing Order ID)         │
│  Duplicates: 45 (already imported)      │
│                                         │
│  [View Import History]  [Import More]   │
└─────────────────────────────────────────┘
```

#### Per-Marketplace Help Text
Each marketplace gets a small instruction card showing where to download CSVs:
- **Meesho**: Supplier Panel → Orders → Download Report
- **Myntra**: Partner Portal → Orders → Export
- **Nykaa**: Seller Dashboard → Orders → Export CSV
- **JioMart**: Seller Portal → My Orders → Export
- **Ajio**: Seller Dashboard → Orders → Download
- **Udaan**: Seller Panel → Orders → Export CSV
- **IndiaMart**: Seller Dashboard → Leads → Download

### 1.6 Marketplace Config Updates

**File: `src/lib/marketplace/config.tsx`**

Add 7 new entries with logos:
- Meesho (#570D57)
- Myntra (#FF3F6C)
- Nykaa (#FC2779)
- JioMart (#0078AD)
- Ajio (#3B3B3B)
- Udaan (#8052EC)
- IndiaMart (#1A6DCC)

**File: `src/app/api/marketplaces/connect/route.ts`**

Add these to the marketplace switch — return `{ requiresCsv: true }` so frontend knows to show import flow instead of OAuth.

### 1.7 Sync Manager Updates

**File: `src/lib/sync/sync-manager.ts`**

Add CSV-only marketplaces to the exclusion list for cron sync (no API to poll):
```typescript
marketplace: {
  in: ["SHOPIFY", "EBAY", ...], // Only API-synced marketplaces
  notIn: ["MEESHO", "MYNTRA", "NYKAA", "JIOMART", "AJIO", "UDAAN", "INDIAMART"],
}
```

### 1.8 Data Freshness Indicator

Since CSV data can be stale, add visual indicators in the dashboard:
- API-synced: "Last synced 5 min ago" (green)
- CSV-imported: "Last imported 2 days ago — upload new CSV for latest data" (amber)

Uses existing `MarketplaceConnection.lastSyncAt` (set to import timestamp).

---

## Phase 2: Browser Extension (Semi-Automated)

**Architecture:**
```
Chrome Extension (installed by seller)
    │
    ├── Content Script: Reads DOM on marketplace seller dashboard
    │   (Meesho/Myntra/etc. orders page)
    │
    ├── Background Script: Sends data to FRAX API
    │   POST /api/import/extension
    │   Headers: { Authorization: Bearer <session-token> }
    │
    └── Popup UI: Shows sync status, last sync time
```

**Key decisions:**
- Extension requests permissions for: `https://supplier.meesho.com/*`, `https://partner.myntra.com/*` etc.
- Reads order/product tables from the DOM (CSS selectors per marketplace)
- Sends batches to FRAX API every 15 minutes
- Uses same UnifiedOrder/UnifiedProduct upsert with `sourceType: "extension"`
- Seller authenticates extension with a one-time token from FRAX dashboard

**How it works for the seller:**
1. Install FRAX Chrome Extension from Chrome Web Store
2. Log into FRAX dashboard → Settings → Generate Extension Token
3. Paste token into extension popup
4. Open Meesho/Myntra seller dashboard as usual
5. Extension silently reads order data and syncs to FRAX in background
6. Dashboard shows "Last synced via extension: 5 min ago"

**Effort:** ~2-3 weeks. Build after CSV is live and validated.

---

## Phase 3: Email Parsing (Fully Automated)

**Architecture:**
```
Seller forwards marketplace emails → FRAX's inbound email address
    │
    ├── Inbound email service (SendGrid Inbound Parse or Resend webhook)
    │
    ├── POST /api/webhooks/email-inbound
    │   Parses email body/subject for order data
    │   Identifies marketplace from sender domain
    │
    └── Upserts to UnifiedOrder with sourceType: "email"
```

**Key decisions:**
- Each user gets a unique forwarding address: `orders-{userId}@import.frax.ai`
- Pattern-match sender domains: `*@meesho.com`, `*@myntra.com`, etc.
- Regex patterns per marketplace to extract order ID, amount, product name from email body
- Fallback: store unparseable emails for manual review

**How it works for the seller:**
1. Go to FRAX Dashboard → Settings → Email Import
2. Copy unique email: `orders-abc123@import.frax.ai`
3. In Gmail, set up a filter: "From meesho.com → Forward to orders-abc123@import.frax.ai"
4. Every order confirmation email gets auto-parsed and imported
5. Dashboard shows real-time order count updating as emails arrive

**Effort:** ~3-4 weeks. Build after browser extension is validated.

---

## Implementation Order (Phase 1)

| Step | What | Files | Effort |
|------|------|-------|--------|
| 1 | Add 7 marketplace types to Prisma enum + `sourceType` field + `CsvImport` model | `prisma/schema.prisma` | 30 min |
| 2 | Run migration | `prisma migrate dev` | 5 min |
| 3 | Install papaparse | `package.json` | 2 min |
| 4 | Create CSV column mappings for all 7 marketplaces | `src/lib/csv/mappings.ts` (new) | 1 hr |
| 5 | Create CSV parser + validator utility | `src/lib/csv/parser.ts` (new) | 1 hr |
| 6 | Create preview API route | `src/app/api/import/preview/route.ts` (new) | 1 hr |
| 7 | Create confirm/import API route | `src/app/api/import/confirm/route.ts` (new) | 2 hr |
| 8 | Create import history API route | `src/app/api/import/history/route.ts` (new) | 30 min |
| 9 | Add marketplace configs (logos, colors) | `src/lib/marketplace/config.tsx` | 30 min |
| 10 | Update connect route for CSV marketplaces | `src/app/api/marketplaces/connect/route.ts` | 15 min |
| 11 | Build import page (upload → preview → confirm) | `src/app/dashboard/import/page.tsx` (new) | 3 hr |
| 12 | Update sync-manager to skip CSV marketplaces | `src/lib/sync/sync-manager.ts` | 10 min |
| 13 | Add data freshness indicators to dashboard | Existing dashboard components | 30 min |
| 14 | Test end-to-end with sample Meesho/Myntra CSVs | Manual testing | 1 hr |

**Total Phase 1 estimate: ~12 hours of implementation**

---

## Verification Plan

1. **Schema**: Run `npx prisma migrate dev` — verify migration succeeds
2. **CSV Parse**: Upload a sample Meesho CSV → verify preview shows correct columns
3. **Import**: Confirm import �� verify UnifiedOrder rows created with `sourceType: "csv"` and `marketplace: "MEESHO"`
4. **Dedup**: Re-upload same file → verify "duplicate file" error (fileHash check)
5. **Dedup rows**: Upload file with overlapping order IDs → verify upsert (no duplicates)
6. **Metrics**: Check dashboard → Meesho orders should appear in revenue totals
7. **Alerts**: Run alert cron → verify Meesho products trigger stockout alerts
8. **AI Chat**: Ask Frax "How are my Meesho sales?" → verify it includes CSV-imported data
9. **Briefing**: Verify weekly briefing includes all marketplace data
10. **Type check**: Run `npx tsc --noEmit` — no new errors
